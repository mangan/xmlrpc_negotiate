#!/usr/bin/python

from distutils.core import setup

setup(
    name="xmlrpc_negotiate",
    description="HTTP Negotiate auth for XMLRPC",
    version="1.0",
    author="mangan",
    py_modules=["xmlrpc_negotiate"])
